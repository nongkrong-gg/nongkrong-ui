import Back from './index';

export default {
  component: Back,
  title: 'Back',
};

export const Primary = {
  args: {
    primary: true,
  },
};

export const Secondary = {
  args: {
    secondary: true,
  },
};
